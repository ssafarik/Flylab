<launch>
	<!-- ========================================================== -->
    <!-- Configuration of the user-interface.                       -->
    <!-- You may comment-out, or add other elements.                -->
    <!--                                                            -->
	<!-- ========================================================== -->


	<!-- Run rviz. -->
	<!-- <node name="rviz" pkg="rviz" type="rviz" args="-d $(find nullmotor)/nullmotor.vcg" /> -->
	<node name="rviz" pkg="rviz" type="rviz" args="-d $(find Fivebar)/Multifly.vcg" />
	
	<!-- Show a camera window. -->
	<node name="image_view" pkg="image_view" type="image_view" args="image:=camera/image_processed" />
</launch>
	